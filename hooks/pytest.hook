#!/bin/bash
uv run pytest
if [ $? -ne 0 ]; then
    if [ $? -ne 5 ]; then
        echo "Pytest could not find any tests to run. If this is unexpected, please check your test configuration."
    else
        echo "Tests failed. Please fix the issues before committing."
        exit 1
    fi
fi